---
title: Methodology
description: How current benchmark results are produced and logged.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## Source of truth

Numbers in the docs should trace to logged artifacts, not screenshots or old run names. The active sources are:

- [experimentlog.md](https://github.com/planetaryescape/cognitive-memory/blob/main/cognitive-memory-benchmarks/experimentlog.md)
- [experimentlog_v2.md](https://github.com/planetaryescape/cognitive-memory/blob/main/cognitive-memory-benchmarks/experimentlog_v2.md)
- the paper source and result paths referenced from those logs

The LongMemEval-S current-refresh rerun is complete and logged as usable.

## Retrieval semantics

All current SDK benchmark runs use the v6-style search pipeline:

<Tabs syncKey="lang">
  <TabItem label="Python">
    ```python
    response = await mem.search(
        question,
        top_k=20,
        deep_recall=True,
        trace=True,
    )
    results = response.results
    ```
  </TabItem>
  <TabItem label="TypeScript">
    ```typescript
    const response = await mem.search({
      query: question,
      limit: 20,
      deepRecall: true,
      trace: true,
    });
    const { results } = response;
    ```
  </TabItem>
</Tabs>

The canonical score is:

```text
score(m, q) = sim(m, q) * R(m)^alpha
```

Default `alpha` is `0.3`. Normal search excludes cold, superseded, and stub records. Deep recall includes cold and superseded records while still excluding stubs.

When reranking is enabled, the engine retrieves `top_k * rerank_factor` candidates, reranks that expanded pool, then returns `top_k`.

## LoCoMo

LoCoMo uses 10 conversations and 1,540 category 1-4 QA items for the standard result. The active Phase 5 v0.5 tuned-default artifact reports:

| Metric | Result |
|--------|:------:|
| Overall token F1 | 46.2% |
| Judge accuracy | 61.3% |

Oracle-evidence analysis reports 63.9% LoCoMo-scoring F1 and 61.1% Mem0-scoring F1.

Phase 14 corrected the category mapping used by later per-category analyses (`1=multi-hop`, `2=temporal`, `3=open-domain`, `4=single-hop`). Use recomputed category tables for mechanism claims.

## LongMemEval-S

The active current-refresh artifact reports:

| Metric | Result |
|--------|:------:|
| Task-averaged accuracy | 71.6% |
| Overall accuracy | 72.6% |
| Abstention accuracy | 90.0% |

This is competitive with ENGRAM-class single-stage systems under the stated protocol. It is not presented as a current SOTA claim.

## LTI-Bench v2

LTI-Bench is a controlled 42-probe harness for lifecycle behavior. It is not a broad generalization benchmark.

| Metric | Result |
|--------|:------:|
| Accuracy | 88.1% |
| F1 | 69.7% |
| Critical fact retention | 100% |

## Historical runs

Older labels such as Run A, Run B, Run F, Run H, Run L, and Run M remain useful in experiment logs and paper-adjacent notes, but public docs should not present them as the active benchmark taxonomy unless the page clearly marks them as historical, pilot, or ablation results.
